<nav class="navbar navbar-expand-lg navbar-dark" style="background: <%= branding?.primaryColor || '#0d9488' %>">
  <div class="container-fluid">
    <span class="navbar-brand d-md-none">
      <i class="bi bi-people-fill me-2"></i>AI Recruiting
    </span>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#topNavbar">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="topNavbar">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">
            <i class="bi bi-person-circle me-1"></i>
            <% if (typeof userName !== 'undefined' && userName) { %>
              <%= userName %>
            <% } else { %>
              Profile
            <% } %>
            <% if (typeof userRole !== 'undefined' && userRole) { %>
              <span class="badge bg-light text-dark ms-1" style="font-size: 0.65rem;"><%= userRole.replace(/_/g, ' ') %></span>
            <% } %>
          </a>
          <ul class="dropdown-menu dropdown-menu-end">
            <li><a class="dropdown-item" href="<%= basePath %>/admin/profile?token=<%= token %>"><i class="bi bi-person me-2"></i>My Profile</a></li>
            <li><a class="dropdown-item" href="<%= basePath %>/admin/account?token=<%= token %>"><i class="bi bi-shield-lock me-2"></i>Account Settings</a></li>
            <li><a class="dropdown-item" href="<%= basePath %>/admin/settings?token=<%= token %>"><i class="bi bi-gear me-2"></i>Settings</a></li>
            <li><hr class="dropdown-divider"></li>
            <li>
              <a class="dropdown-item text-danger" href="<%= basePath %>/" onclick="localStorage.clear(); document.cookie='authToken=; path=/; expires=Thu, 01 Jan 1970 00:00:00 GMT';">
                <i class="bi bi-box-arrow-left me-2"></i>Logout
              </a>
            </li>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>
